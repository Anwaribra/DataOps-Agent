version: 2

models:
  - name: int_customer_orders
    description: "Aggregated order metrics by customer"
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
          - relationships:
              to: ref('stg_customers')
              field: customer_id
